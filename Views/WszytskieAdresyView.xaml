<local:WszystkieViewBase x:Class="Projekt.Views.WszytskieAdresyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="IdAdres" Binding="{Binding Path=IdAdres}"/>
                <DataGridTextColumn Header="Kod pocztowy" Binding="{Binding Path=KodPocztowy}"/>
                <DataGridTextColumn Header="Kraj" Binding="{Binding Path=Kraj}"/>
                <DataGridTextColumn Header="Miasto" Binding="{Binding Path=Miasto}"/>
                <DataGridTextColumn Header="Ulica" Binding="{Binding Path=Ulica}"/>
                <DataGridTextColumn Header="Nr domu" Binding="{Binding Path=NrDomu}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>

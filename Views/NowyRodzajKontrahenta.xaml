<local:JedenViewBase x:Class="Projekt.Views.NowyRodzajKontrahenta"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>
        <Label Content="Nazwa" Grid.Column="0" Grid.Row="0"/>
        <TextBox Grid.Column="1"  Width="200" Text="{Binding Path=Nazwa , UpdateSourceTrigger=PropertyChanged}" />

        <Label Content="Opis" Grid.Column="0" Grid.Row="1" />
        <TextBox Grid.Column="1" Grid.Row="1" Margin="0,2,0,0"  Text="{Binding Path=Opis , UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</local:JedenViewBase>
